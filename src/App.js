import React from 'react';
import { Box } from '@material-ui/core';
import Header from './components/Header';

const App = () => {
  return (
    <Box px={0}>
      <Header />
    </Box>
  );
};

export default App;
